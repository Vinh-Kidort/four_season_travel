
app.search.meilisearch.enabled=false

# Refresh cookie settings for cross-site HTTPS frontend/backend.
app.auth.refresh-cookie.secure=true
app.auth.refresh-cookie.same-site=None

# MongoDB Atlas — Atlas Search tự động dùng connection string
# Không cần config thêm gì cho Atlas Search
